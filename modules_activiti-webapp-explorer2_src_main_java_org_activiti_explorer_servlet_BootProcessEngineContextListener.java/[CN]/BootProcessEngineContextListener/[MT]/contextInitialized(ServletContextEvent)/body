{
  this.processEngine=WebApplicationContextUtils.getRequiredWebApplicationContext(servletContextEvent.getServletContext()).getBean(ProcessEngine.class);
  initFormPropertyMapping();
  initUriNavigation();
  initAttachmentRenderers();
  initDemoData();
}
