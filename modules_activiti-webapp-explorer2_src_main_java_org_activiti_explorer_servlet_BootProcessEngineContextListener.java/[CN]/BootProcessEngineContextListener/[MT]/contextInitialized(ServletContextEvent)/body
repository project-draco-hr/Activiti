{
  ProcessEngines.init();
  if (ProcessEngines.getDefaultProcessEngine() == null) {
    throw new ActivitiException("Could not construct a process engine. " + "Please verify that your activiti.cfg.xml configuration is correct.");
  }
  initFormPropertyMapping();
  initUriNavigation();
  initDemoData();
}
