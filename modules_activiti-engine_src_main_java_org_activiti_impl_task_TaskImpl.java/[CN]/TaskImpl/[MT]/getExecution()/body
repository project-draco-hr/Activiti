{
  if ((execution == null) && (executionId != null)) {
    this.execution=CommandContextHolder.getCurrentCommandContext().getPersistenceSession().findExecution(executionId);
  }
  return execution;
}
