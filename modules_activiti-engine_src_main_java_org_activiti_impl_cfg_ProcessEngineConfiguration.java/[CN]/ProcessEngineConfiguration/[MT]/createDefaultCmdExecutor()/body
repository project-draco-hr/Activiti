{
  InterceptorChainBuilder builder=new InterceptorChainBuilder().addInterceptor(new CommandContextInterceptor(this)).addInterceptor(new CommandExecutorImpl());
  CommandExecutor commandExecutor=builder.getFirst();
  return commandExecutor;
}
