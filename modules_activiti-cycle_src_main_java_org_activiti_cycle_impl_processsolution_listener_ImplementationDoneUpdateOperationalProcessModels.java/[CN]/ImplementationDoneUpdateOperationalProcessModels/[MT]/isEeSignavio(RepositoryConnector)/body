{
  SignavioConnectorInterface connectorInterface=(SignavioConnectorInterface)connector;
  return !connectorInterface.getConfiguration().getSignavioUrl().endsWith("activiti-modeler/") && !connectorInterface.getConfiguration().getSignavioUrl().endsWith("activiti-modeler");
}
