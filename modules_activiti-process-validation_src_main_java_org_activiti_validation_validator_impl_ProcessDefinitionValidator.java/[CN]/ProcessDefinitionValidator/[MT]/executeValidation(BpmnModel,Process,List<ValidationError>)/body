{
  if (!process.isExecutable()) {
    addError(errors,Problems.PROCESS_DEFINITION_IS_NOT_EXECUTABLE,process,process,"Process definition should be executable (property 'isExecutable' on process)");
  }
}
