{
  if (group == null) {
    throw new ActivitiException("group is null");
  }
  if (group.getRevision() == 0) {
    commandContext.getIdentitySession().insertGroup(group);
  }
 else {
    commandContext.getIdentitySession().updateGroup(group);
  }
  return null;
}
