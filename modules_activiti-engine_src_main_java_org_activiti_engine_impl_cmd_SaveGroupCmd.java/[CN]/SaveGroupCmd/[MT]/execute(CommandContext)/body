{
  if (group == null) {
    throw new ActivitiException("group is null");
  }
  if (group.getRevision() == 0) {
    commandContext.getGroupManager().insertGroup(group);
  }
 else {
    commandContext.getGroupManager().updateGroup(group);
  }
  return null;
}
