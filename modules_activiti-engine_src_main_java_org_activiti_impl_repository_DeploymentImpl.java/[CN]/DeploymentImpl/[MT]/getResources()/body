{
  if (!resourcesInitialized) {
    List<ByteArrayImpl> resourceList=CommandContextHolder.getCurrentCommandContext().getPersistenceSession().findDeploymentResources(id);
    resources=new HashMap<String,ByteArrayImpl>();
    for (    ByteArrayImpl resource : resourceList) {
      resources.put(resource.getName(),resource);
    }
  }
  return resources;
}
