{
  HistoricProcessInstanceEntity historicProcessInstance=CommandContext.getCurrent().getHistorySession().findHistoricProcessInstance(execution.getId());
  ExecutionEntity executionEntity=(ExecutionEntity)execution;
  historicProcessInstance.markEnded(ClockUtil.getCurrentTime(),executionEntity.getActivityId());
}
