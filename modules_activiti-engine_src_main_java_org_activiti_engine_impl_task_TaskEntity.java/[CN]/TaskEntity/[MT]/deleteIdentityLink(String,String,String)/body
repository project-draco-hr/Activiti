{
  List<IdentityLinkEntity> identityLinks=Context.getCommandContext().getTaskSession().findIdentityLinkByTaskUserGroupAndType(id,userId,groupId,type);
  for (  IdentityLinkEntity identityLink : identityLinks) {
    identityLink.delete();
  }
}
