{
  if (!isIdentityLinksInitialized) {
    taskIdentityLinkEntities=CommandContext.getCurrent().getTaskSession().findIdentityLinksByTaskId(id);
    isIdentityLinksInitialized=true;
  }
  return taskIdentityLinkEntities;
}
