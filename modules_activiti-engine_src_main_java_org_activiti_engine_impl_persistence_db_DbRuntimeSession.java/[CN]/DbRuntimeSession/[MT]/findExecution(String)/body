{
  DbExecutionImpl execution=(DbExecutionImpl)dbSqlSession.selectOne("selectExecution",executionId);
  if (execution != null) {
    execution=(DbExecutionImpl)loaded.add(execution);
  }
  return execution;
}
