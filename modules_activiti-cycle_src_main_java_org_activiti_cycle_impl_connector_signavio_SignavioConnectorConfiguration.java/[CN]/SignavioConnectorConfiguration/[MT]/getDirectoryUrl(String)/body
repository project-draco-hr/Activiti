{
  if (id.startsWith("/")) {
    return getRepositoryBackendUrl() + DIRECTORY_URL_SUFFIX + encode(id);
  }
 else {
    return getRepositoryBackendUrl() + DIRECTORY_URL_SUFFIX + "/"+ encode(id);
  }
}
