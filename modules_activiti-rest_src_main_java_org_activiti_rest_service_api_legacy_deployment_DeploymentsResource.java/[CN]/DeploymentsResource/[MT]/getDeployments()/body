{
  if (authenticate() == false)   return null;
  DataResponse response=new LegacyDeploymentsPaginateList().paginateList(getQuery(),ActivitiUtil.getRepositoryService().createDeploymentQuery(),"id",properties);
  return response;
}
