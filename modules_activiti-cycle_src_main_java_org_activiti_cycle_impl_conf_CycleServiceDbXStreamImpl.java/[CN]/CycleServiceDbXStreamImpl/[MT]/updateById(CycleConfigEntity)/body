{
  SqlSessionFactory sqlMapper=getSessionFactory();
  SqlSession session=sqlMapper.openSession();
  try {
    session.update("org.activiti.cycle.impl.conf.CycleConfigEntity.updateCycleConfigById",cycleConfig);
    session.commit();
  }
  finally {
    session.close();
  }
}
