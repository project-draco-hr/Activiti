{
  SqlSessionFactory sqlMapper=getSessionFactory();
  SqlSession session=sqlMapper.openSession();
  try {
    return (CycleConfigEntity)session.selectOne("org.activiti.cycle.impl.conf.CycleConfigEntity.selectCycleConfigById",id);
  }
  finally {
    session.close();
  }
}
