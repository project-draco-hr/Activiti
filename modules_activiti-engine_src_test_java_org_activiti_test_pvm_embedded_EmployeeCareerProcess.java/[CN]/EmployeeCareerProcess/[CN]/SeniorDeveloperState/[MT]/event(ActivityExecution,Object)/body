{
  String eventString=(String)event;
  if (eventString.contains("years of experience") && getEmployee(execution).getNrOfSucessFulProjects() == 2) {
    if (Integer.valueOf(eventString.charAt(0)) >= 5) {
      execution.take("enoughExperience");
    }
  }
}
