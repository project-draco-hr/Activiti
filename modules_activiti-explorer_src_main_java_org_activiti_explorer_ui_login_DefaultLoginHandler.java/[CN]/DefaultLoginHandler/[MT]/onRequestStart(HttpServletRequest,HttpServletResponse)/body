{
  if (ExplorerApp.get().getLoggedInUser() == null || request.getSession(false) == null || request.getSession().getAttribute(Constants.AUTHENTICATED_USER_ID) != null)   return;
  request.getSession().setAttribute(Constants.AUTHENTICATED_USER_ID,ExplorerApp.get().getLoggedInUser().getId());
}
