{
  if ((processDefinition == null) && (processDefinitionId != null)) {
    processDefinition=CommandContext.getCurrent().getProcessCache().findProcessDefinitionById(processDefinitionId);
  }
  return processDefinition;
}
