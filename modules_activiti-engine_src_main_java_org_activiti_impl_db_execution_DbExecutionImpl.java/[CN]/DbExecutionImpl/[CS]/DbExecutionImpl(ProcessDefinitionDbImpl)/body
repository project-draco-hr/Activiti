{
  super(processDefinition);
  this.isNew=true;
  this.executions=new ArrayList<ExecutionImpl>();
  this.isExecutionsInitialized=true;
  CommandContextHolder.getCurrentCommandContext().getPersistenceSession().insert(this);
}
