{
  ArrayList<RepositoryArtifact> list=new ArrayList<RepositoryArtifact>();
  for (  RepositoryNode node : getChildren()) {
    if (node instanceof RepositoryArtifact)     list.add((RepositoryArtifact)node);
  }
  return list;
}
