{
  CommandExecutor commandExecutor=commandContext.getProcessEngineConfiguration().getCommandExecutorTxRequired();
  return commandContext.getIdentitySession().createNewGroupQuery(commandExecutor);
}
