{
  resetThreadContext();
  setActor();
  boolean flush=getAssociationManager().isFlushBeanStore();
  if (flush) {
    flushBeanStore();
  }
  T result=next.execute(command);
  if (flush) {
    flushBeanStore();
  }
  resetThreadContext();
  return result;
}
