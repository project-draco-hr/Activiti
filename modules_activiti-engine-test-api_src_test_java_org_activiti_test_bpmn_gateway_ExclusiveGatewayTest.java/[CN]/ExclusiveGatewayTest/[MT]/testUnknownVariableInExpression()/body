{
  exception.expect(ActivitiException.class);
  exception.expectMessage("Unknown property used in expression");
  deployer.getProcessService().startProcessInstanceByKey("exclusiveGwDiverging",CollectionUtil.singletonMap("iinput",1));
}
