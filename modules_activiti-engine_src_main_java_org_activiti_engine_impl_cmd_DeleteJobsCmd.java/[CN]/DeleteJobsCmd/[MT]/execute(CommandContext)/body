{
  JobEntity jobToDelete=null;
  for (  String jobId : jobIds) {
    jobToDelete=Context.getCommandContext().getJobEntityManager().findJobById(jobId);
    if (jobToDelete != null) {
      jobToDelete.delete();
    }
  }
  return null;
}
