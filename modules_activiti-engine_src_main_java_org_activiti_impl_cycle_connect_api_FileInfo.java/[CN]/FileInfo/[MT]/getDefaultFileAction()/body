{
  if (cachedDefaultFileAction == null) {
    getActions();
  }
  log.info("Default actions for file type " + getFileType() + " requested, returning "+ (cachedDefaultFileAction == null ? "null" : cachedDefaultFileAction.getName()));
  return cachedDefaultFileAction;
}
