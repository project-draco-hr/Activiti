{
  List<GroupImpl> groups=CommandContextHolder.getCurrentCommandContext().getPersistenceSession().findGroupsByUser(candidateUser);
  List<String> groupIds=new ArrayList<String>();
  for (  GroupImpl group : groups) {
    groupIds.add(group.getId());
  }
  return groupIds;
}
