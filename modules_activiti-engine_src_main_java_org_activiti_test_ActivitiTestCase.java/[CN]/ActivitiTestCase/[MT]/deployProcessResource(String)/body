{
  Deployment deployment=processEngine.getProcessService().newDeployment().name(resource).addClasspathResource(resource).deploy();
  registerDeployment(deployment.getId());
  return deployment.getId();
}
