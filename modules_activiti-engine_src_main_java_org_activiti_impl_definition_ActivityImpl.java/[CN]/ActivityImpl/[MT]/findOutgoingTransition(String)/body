{
  for (  TransitionImpl transition : outgoingTransitions) {
    if (((transitionId == null) && (transition.getId() == null)) || (transitionId.equals(transition.getId()))) {
      return transition;
    }
  }
  return null;
}
