{
  if (deploymentId == null) {
    throw new ActivitiException("deploymentId is null");
  }
  return Context.getCommandContext().getDeploymentEntityManager().getDeploymentResourceNames(deploymentId);
}
