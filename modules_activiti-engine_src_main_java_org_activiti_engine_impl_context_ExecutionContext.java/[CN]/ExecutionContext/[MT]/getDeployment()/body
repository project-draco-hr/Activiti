{
  String deploymentId=getProcessDefinition().getDeploymentId();
  DeploymentEntity deployment=Context.getCommandContext().getRepositorySession().findDeploymentById(deploymentId);
  return deployment;
}
