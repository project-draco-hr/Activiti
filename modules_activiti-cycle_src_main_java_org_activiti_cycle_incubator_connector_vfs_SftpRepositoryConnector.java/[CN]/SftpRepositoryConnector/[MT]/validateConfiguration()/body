{
  if (getConfiguration().getProtocol() == null)   throw new RepositoryException("No protocol specified");
  if (!getConfiguration().getProtocol().equals("sftp"))   throw new RepositoryException("Protocol needs to be sftp");
  if (getConfiguration().getHostname() == null)   throw new RepositoryException("No hostname specified");
}
