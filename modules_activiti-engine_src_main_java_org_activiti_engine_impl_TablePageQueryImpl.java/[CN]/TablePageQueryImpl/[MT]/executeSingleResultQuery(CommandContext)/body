{
  if (tableName == null || start == -1 || maxRows == -1) {
    throw new ActivitiException("Table name, offset and maxResults are " + "minimally needed to execute a TablePageQuery");
  }
  return commandContext.getManagementSession().getTablePage(tableName,start,maxRows,sortColumn,sortOrder);
}
