{
  if (owner == null && this.owner == null) {
    return;
  }
  this.owner=owner;
  CommandContext commandContext=Context.getCommandContext();
  if (commandContext != null) {
    commandContext.getHistoricTaskInstanceManager().setTaskOwner(id,owner);
  }
}
