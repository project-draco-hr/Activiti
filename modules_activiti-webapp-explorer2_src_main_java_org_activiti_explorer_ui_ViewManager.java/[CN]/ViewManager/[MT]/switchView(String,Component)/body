{
  Component newView=null;
  if (component != null) {
    newView=component;
    views.put(viewName,component);
  }
 else {
    newView=views.get(viewName);
  }
  mainLayout.addComponent(newView,Constants.LOCATION_CONTENT);
}
