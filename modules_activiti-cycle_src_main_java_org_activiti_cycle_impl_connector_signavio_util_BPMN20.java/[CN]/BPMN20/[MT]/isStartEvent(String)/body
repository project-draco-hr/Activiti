{
  if (NONE_START_EVENT.equals(stencilId) || CONDITIONAL_START_EVENT.equals(stencilId) || MESSAGE_START_EVENT.equals(stencilId)|| MULTIPLE_START_EVENT.equals(stencilId)|| SIGNAL_START_EVENT.equals(stencilId)|| TIMER_START_EVENT.equals(stencilId)|| ESCALATION_START_EVENT.equals(stencilId)|| ERROR_START_EVENT.equals(stencilId)|| COMPENSATION_START_EVENT.equals(stencilId)|| PARALLEL_MULTIPLE_START_EVENT.equals(stencilId)) {
    return true;
  }
  return false;
}
