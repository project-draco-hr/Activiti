{
  if (processDefinitionId != null && processDefinition == null) {
    processDefinition=CommandContext.getCurrent().getRepositorySession().findDeployedProcessDefinitionById(processDefinitionId);
  }
  return (ProcessDefinitionEntity)processDefinition;
}
