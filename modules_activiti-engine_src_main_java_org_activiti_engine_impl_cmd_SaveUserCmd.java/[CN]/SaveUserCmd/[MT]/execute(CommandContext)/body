{
  if (user == null) {
    throw new ActivitiException("user is null");
  }
  if (user.getRevision() == 0) {
    commandContext.getUserManager().insertUser(user);
  }
 else {
    commandContext.getUserManager().updateUser(user);
  }
  return null;
}
