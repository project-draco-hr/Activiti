{
  for (  PersistentObject insertedObject : insertedObjects) {
    if ((clazz == insertedObject.getClass()) && (id.equals(insertedObject.getId()))) {
      insertedObjects.remove(insertedObject);
      return;
    }
  }
  deletedObjects.add(new DeleteById(clazz,id));
}
