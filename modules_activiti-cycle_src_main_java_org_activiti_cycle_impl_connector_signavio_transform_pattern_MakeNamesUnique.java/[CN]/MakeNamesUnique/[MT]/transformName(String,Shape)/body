{
  int counter=1;
  String newName=name;
  while (existingNames.contains(newName)) {
    counter++;
    newName=name + prefix + counter+ suffix;
  }
  existingNames.add(newName);
  return newName;
}
