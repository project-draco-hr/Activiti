{
  if ((activityInstance == null) && (activityInstanceId != null)) {
    this.activityInstance=CommandContext.getCurrent().getRuntimeSession().findExecution(activityInstanceId);
  }
  return activityInstance;
}
