{
  if ((activityInstance == null) && (activityInstanceId != null)) {
    this.activityInstance=CommandContext.getCurrent().getRuntimeSession().findActivityInstanceById(activityInstanceId);
  }
  return activityInstance;
}
