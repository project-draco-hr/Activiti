{
  if ((activityInstance == null) && (activityInstanceId != null)) {
    this.activityInstance=CommandContext.getCurrent().getPersistenceSession().findExecution(activityInstanceId);
  }
  return activityInstance;
}
