{
  ClientResource client=getAuthenticatedClient(url);
  try {
    client.post(body);
    fail();
  }
 catch (  Exception e) {
    assertEquals(status,client.getResponse().getStatus());
  }
  client.release();
}
