{
  if (cachedDefaultFileAction == null) {
    getActions();
  }
  log.info("Default actions for file type " + getArtifactType() + " requested, returning "+ (cachedDefaultFileAction == null ? "null" : cachedDefaultFileAction.getName()));
  return cachedDefaultFileAction;
}
