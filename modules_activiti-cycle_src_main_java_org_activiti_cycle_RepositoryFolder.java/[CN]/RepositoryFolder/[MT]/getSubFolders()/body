{
  ArrayList<RepositoryFolder> list=new ArrayList<RepositoryFolder>();
  for (  RepositoryNode node : getChildren()) {
    if (node instanceof RepositoryFolder)     list.add((RepositoryFolder)node);
  }
  return list;
}
