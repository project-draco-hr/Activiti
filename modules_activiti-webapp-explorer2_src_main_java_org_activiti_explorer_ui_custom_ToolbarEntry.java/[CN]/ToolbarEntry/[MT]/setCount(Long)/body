{
  this.count=count;
  if (count != null) {
    countButton.setCaption(count + "");
    if (!countButton.isVisible()) {
      countButton.setVisible(true);
    }
  }
 else {
    countButton.setVisible(true);
  }
}
