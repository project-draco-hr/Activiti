{
  try {
    String factoryClassName=System.getProperty(ExpressionFactory.class.getName());
    if (factoryClassName != null && !"".equals(factoryClassName)) {
      return factoryClassName;
    }
  }
 catch (  SecurityException se) {
  }
  return null;
}
