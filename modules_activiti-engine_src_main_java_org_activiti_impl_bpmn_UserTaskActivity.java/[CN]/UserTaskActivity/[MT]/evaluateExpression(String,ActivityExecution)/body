{
  ScriptingEngines scriptingEngines=CommandContext.getCurrent().getScriptingEngines();
  return (String)scriptingEngines.evaluate(expr,ScriptingEngines.DEFAULT_EXPRESSION_LANGUAGE,(ExecutionImpl)execution);
}
