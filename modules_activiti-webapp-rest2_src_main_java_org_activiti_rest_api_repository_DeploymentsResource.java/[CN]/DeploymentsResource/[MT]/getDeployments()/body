{
  if (authenticate() == false)   return null;
  DataResponse response=new DeploymentsPaginateList().paginateList(getQuery(),ActivitiUtil.getRepositoryService().createDeploymentQuery(),"id",properties);
  return response;
}
