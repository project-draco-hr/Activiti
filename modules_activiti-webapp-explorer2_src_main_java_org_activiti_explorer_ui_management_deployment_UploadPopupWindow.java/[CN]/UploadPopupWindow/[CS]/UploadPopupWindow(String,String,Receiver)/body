{
  addStyleName(Reindeer.WINDOW_LIGHT);
  setModal(true);
  center();
  setCaption(caption);
  initLayout();
  addDescription(description);
  addUpload(receiver);
  addOrLabel();
  addDropPanel();
}
