{
  RuntimeSession runtimeSession=commandContext.getRuntimeSession();
  if (groupType == null) {
    return runtimeSession.findGroupsByUser(userId);
  }
  return runtimeSession.findGroupsByUserAndType(userId,groupType);
}
