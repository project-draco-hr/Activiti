{
  Content c=new Content();
  addValueToContent(c,connector,artifact);
  if (c.isNull()) {
    throw new RepositoryException("No content created for artifact " + artifact.getId() + " ' by provider '"+ this.getClass().getName()+ "' (was null). Please check provider or artifact.");
  }
  return c;
}
