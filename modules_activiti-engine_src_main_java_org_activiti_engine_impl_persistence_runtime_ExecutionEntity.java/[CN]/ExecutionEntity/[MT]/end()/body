{
  super.end();
  ensureVariablesInitialized();
  variables.clear();
  List<TaskEntity> tasks=CommandContext.getCurrent().getTaskSession().findTasksByExecutionId(id);
  for (  TaskEntity task : tasks) {
    task.delete();
  }
  CommandContext.getCurrent().getDbSqlSession().delete(this);
}
