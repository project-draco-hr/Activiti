{
  if (userId == null) {
    throw new ActivitiException("userId is null");
  }
  if (groupId == null) {
    throw new ActivitiException("groupId is null");
  }
  commandContext.getMembershipManager().createMembership(userId,groupId);
  return null;
}
