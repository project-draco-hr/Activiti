{
  try {
    processEngineConfiguration.getCommandExecutor().execute(new ControlledCommand(activeThread,new SignalCmd(executionId,null,null)));
  }
 catch (  ActivitiOptimisticLockingException e) {
    this.exception=e;
  }
  log.fine(getName() + " ends");
}
