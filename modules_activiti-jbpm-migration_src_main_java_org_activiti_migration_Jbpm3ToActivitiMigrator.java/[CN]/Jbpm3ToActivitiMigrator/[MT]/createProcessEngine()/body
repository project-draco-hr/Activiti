{
  return StandaloneProcessEngineConfiguration.createStandaloneProcessEngineConfiguration().setDatabaseType(activitiDbType).setJdbcDriver(activitiDbDriver).setJdbcUrl(activitiDbUrl).setJdbcUsername(activitiDbUser).setJdbcPassword(activitiDbPassword).setDatabaseSchemaUpdate("true").buildProcessEngine();
}
