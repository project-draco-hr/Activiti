{
  if (pictureByteArrayId != null && pictureByteArray == null) {
    pictureByteArray=Context.getCommandContext().getByteArrayEntityManager().findById(pictureByteArrayId);
  }
  return pictureByteArray;
}
