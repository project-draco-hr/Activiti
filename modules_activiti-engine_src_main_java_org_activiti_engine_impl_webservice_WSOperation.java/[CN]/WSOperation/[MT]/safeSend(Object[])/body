{
  Object[] results=null;
  try {
    results=this.service.getClient().send(this.name,arguments);
  }
 catch (  Exception e) {
    throw e;
  }
  if (results == null) {
    results=new Object[]{};
  }
  return results;
}
