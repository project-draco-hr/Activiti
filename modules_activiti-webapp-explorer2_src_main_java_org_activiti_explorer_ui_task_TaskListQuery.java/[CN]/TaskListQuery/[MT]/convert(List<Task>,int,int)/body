{
  List<TaskListEntry> taskListEntries=new ArrayList<TaskListEntry>();
  for (int i=0; i < tasks.size(); i++) {
    taskListEntries.add(new TaskListEntry(taskTable,startIndex + i,tasks.get(i)));
  }
  return taskListEntries;
}
