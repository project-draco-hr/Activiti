{
  PersistenceSession persistenceSession=commandContext.getPersistenceSession();
  DbExecutionImpl execution=persistenceSession.findExecution(executionId);
  execution.setVariables(variables);
  return null;
}
