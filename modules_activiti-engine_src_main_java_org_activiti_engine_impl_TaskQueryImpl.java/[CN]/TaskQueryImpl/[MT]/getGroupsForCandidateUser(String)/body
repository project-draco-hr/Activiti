{
  List<GroupEntity> groups=CommandContext.getCurrent().getIdentitySession().findGroupsByUser(candidateUser);
  List<String> groupIds=new ArrayList<String>();
  for (  GroupEntity group : groups) {
    groupIds.add(group.getId());
  }
  return groupIds;
}
