{
  Execution execution=getProcessInstanceFromRequest(processInstanceId);
  return createExecutionVariable(execution,true,RestResponseFactory.VARIABLE_PROCESS,getServerRootUrl(request),request,response);
}
