{
  assertThat("An active execution with id " + processInstanceId + " was found when expecting none (it should have ended and been removed).",processEngine.getRuntimeService().findProcessInstanceById(processInstanceId),nullValue());
}
