{
  FormField field=new FormField();
  field.setId(id);
  if (labelId != null) {
    field.setLabelId(labelId);
  }
  if (set != null) {
    field.setSet(set);
  }
  addFormAppearanceElement(field);
  return field;
}
