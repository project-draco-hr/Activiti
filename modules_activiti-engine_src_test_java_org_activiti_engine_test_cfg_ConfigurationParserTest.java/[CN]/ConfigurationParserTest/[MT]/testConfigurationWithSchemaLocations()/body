{
  ConfigurationParse parse=configurationParser.createParse().sourceResource("org/activiti/engine/test/cfg/activiti.cfg.with.schemalocation.xml").execute();
  assertNotNull(parse);
  assertEquals("with-schema",parse.getProcessEngineName());
}
