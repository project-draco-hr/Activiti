{
  Runnable verifier=new Runnable(){
    public void run(){
      Assert.assertNull("An active execution with id " + processInstanceId + " was found.",processEngine.getProcessService().findProcessInstanceById(processInstanceId));
    }
  }
;
  verifiers.add(verifier);
}
