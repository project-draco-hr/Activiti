{
  Runnable verifier=new Runnable(){
    public void run(){
      assertThat("An active execution with id " + processInstanceId + " was found.",processEngine.getProcessService().findProcessInstanceById(processInstanceId),nullValue());
    }
  }
;
  verifiers.add(verifier);
}
