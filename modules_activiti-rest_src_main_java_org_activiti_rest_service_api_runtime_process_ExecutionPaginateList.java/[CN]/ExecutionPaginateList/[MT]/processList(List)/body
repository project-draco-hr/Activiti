{
  List<ExecutionResponse> responseList=new ArrayList<ExecutionResponse>();
  for (  Object instance : list) {
    responseList.add(restResponseFactory.createExecutionResponse((Execution)instance,serverRootUrl));
  }
  return responseList;
}
