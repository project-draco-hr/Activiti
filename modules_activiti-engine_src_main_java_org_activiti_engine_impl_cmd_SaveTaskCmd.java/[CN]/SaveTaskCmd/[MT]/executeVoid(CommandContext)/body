{
  if (task.getId() == null) {
    commandContext.getDbSqlSession().insert(task);
  }
 else {
    TaskEntity persistentTask=commandContext.getTaskSession().findTaskById(task.getId());
    persistentTask.update(task);
  }
  commandContext.getDbSqlSession().insert((TaskEntity)task);
}
