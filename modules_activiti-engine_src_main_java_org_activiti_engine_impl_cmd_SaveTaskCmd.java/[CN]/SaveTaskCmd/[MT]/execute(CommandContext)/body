{
  if (task == null) {
    throw new ActivitiIllegalArgumentException("task is null");
  }
  if (task.getRevision() == 0) {
    task.insert(null);
  }
 else {
    task.update();
  }
  return null;
}
