{
  super.initUi();
  getToolBar().setActiveEntry(ManagementMenuBar.ENTRY_DATABASE);
  populateTableList();
  if (tableName == null) {
    selectListElement(0);
  }
 else {
    table.select(tableName);
  }
}
